---
title: Зачем нужна идемпотентность
description: Во что обходится ретрай, если сервер не был рассчитан на то, что его повторят.
problem: >-
  Таймаут — это не отказ. Клиент повторяет запрос, платёж проходит дважды,
  и баг вылезает только на проде под нагрузкой.
tags: [distributed-systems, api-design, reliability]
order: 1
deck: idempotency
steps:
  - id: naive
    title: Обработчик, который выглядит нормально
    narration: >-
      Списали, записали строку, вернули id. На ревью на этом коде никто не остановится.
      И он правда корректный — ровно для одного выполнения.
    note: >-
      Один вызов, одно списание. Верно ровно для одного выполнения.
  - id: timeout
    title: Сеть с этим не согласна
    narration: >-
      Клиент отваливается по таймауту через три секунды. Но таймаут не говорит ничего о
      том, что произошло на сервере: запрос мог не дойти, а мог выполниться — и потеряться
      уже на обратном пути. Различить эти случаи клиент не может, поэтому повторяет запрос.
      И второе списание проходит.
    note: >-
      Ответ потерян, а деньги ушли.
  - id: key
    title: Именуем намерение, а не запрос
    narration: >-
      Починка начинается на клиенте. Он генерирует ключ один раз и переиспользует его во
      всех ретраях одного намерения. Обрати внимание, что именно идентифицирует ключ:
      «одно списание», а не «один HTTP-запрос». Ключ, сгенерированный сервером, был бы
      бесполезен — он был бы новым на каждой попытке, то есть ровно тем, что ломается.
    note: >-
      Ключ рождается здесь — он обязан пережить запрос.
  - id: store
    title: Уносим ограничение в базу
    narration: >-
      Ключу нужно место, которое все реплики сервиса видят одновременно. Первичный ключ
      таким местом является, map в памяти процесса — нет. В этом весь фокус: ты берёшь
      в долг гарантию уникальности у базы, вместо того чтобы изобретать свою.
    note: >-
      Уникальность не твоя, а базы.
  - id: handler
    title: Сначала заявка на ключ, потом деньги
    narration: >-
      Порядок здесь принципиален. Заявка на ключ делается до вызова банка, чтобы дубль
      проиграл гонку на вставке и до платёжного провайдера просто не доехал. Две детали
      легко упустить - сравнение хеша запроса ловит клиента, который переиспользовал один
      ключ для разных тел, а занятый, но недоделанный ключ должен отдавать 409, а не
      пустой успех.
    note: >-
      Повтор доехал до сервиса, но не до провайдера.
quiz:
  - question: Где должен генерироваться ключ идемпотентности?
    options:
      - На сервере, в момент прихода запроса
      - На клиенте, один раз на одну задуманную операцию
      - В отдельном сервисе ключей, к которому ходят обе стороны
    answer: 1
    explanation: >-
      Для сервера ретрай — это новый запрос, поэтому серверный ключ был бы каждый раз
      другим и не дедуплицировал бы ничего. Ключ должен жить дольше отдельного запроса,
      а значит владеть им может только клиент.
  - question: Почему заявку на ключ делают до вызова банка, а не после?
    options:
      - Так быстрее — на счастливом пути на один round trip меньше
      - Иначе остаётся окно, в котором два параллельных запроса спишут оба
      - Банк требует, чтобы ключ уже существовал
    answer: 1
    explanation: >-
      Если сначала списать, а ключ записать потом, два параллельных дубля успеют пройти
      проверку оба и оба доедут до провайдера. Заявка первой заставляет базу выбрать
      победителя до того, как сдвинутся деньги.
---

Повторный запрос — это не редкий случай. Мобильные сети теряют ответы, балансировщики
отваливаются по таймауту, консьюмеры очередей передоставляют сообщения, а пользователи
жмут кнопку дважды. Любую ручку, меняющую состояние, рано или поздно позовут второй раз
с тем же намерением.

Идемпотентность — это свойство, которое позволяет перестать об этом волноваться: второй
вызов даёт тот же результат, что и первый, и не добавляет побочных эффектов.

## Где это вылезает

Платежи — очевидный пример, потому что ущерб измеряется в деньгах. Но та же форма
повторяется в письмах о регистрации, доставке вебхуков, создании заказа и вообще везде,
где есть очередь с семантикой at-least-once.

## Чем это не является

Идемпотентность — не то же самое, что защита ретраев блокировкой. Блокировка запрещает
одновременное выполнение и ничего не делает с ретраем, который пришёл через десять минут.
И это не дедупликация по телу запроса — два действительно разных платежа на одну сумму
одному и тому же продавцу пользователь имеет полное право совершить.
