---
title: Why aggregate versioning
description: ''
problem: >-
  Два вызова прочитали один счёт, оба применили своё правило, оба сохранили. В
  базе осталось изменение того, кто записал вторым — первое исчезло, и ни одна
  транзакция об этом не узнала: оба `UPDATE` прошли успешно.
tags: [ddd, concurrency, pattern]
order: 11
deck: why-aggregate-versioning
series: why-ddd
labels:
  kindCall: вызов
  first: Оплата
  firstDoes: отметила счёт оплаченным
  second: Просрочка
  secondDoes: начислила пеню
  sameState: оба прочитали одно состояние
  lastWins: осталось то, что записали вторым
terms:
  потерянное обновление: >-
    Изменение, которое исчезло не из-за ошибки, а потому что второй вызов
    записал поверх состояние, прочитанное до первого.
  оптимистичная блокировка: >-
    Запись проверяет, что состояние не изменилось с момента чтения, и
    отказывается, если изменилось. Ничего не запирает — только обнаруживает.
  пессимистичная блокировка: >-
    Строка запирается на время транзакции, и остальные ждут. Конфликт не
    возникает, но и работать по этой строке параллельно нельзя.
  агрегат: >-
    Кластер объектов, который меняется как одно целое, с корнем, через который
    к нему обращаются снаружи. Граница согласованности и граница транзакции.
  unit of work: >-
    Порт, который выполняет переданную работу целиком или не выполняет вовсе.
    Слова «транзакция» в домене при этом нет.
  outbox: >-
    Таблица, в которую факт пишется той же транзакцией, что и изменение. На
    шину его отправляет отдельный процесс, читая эту таблицу.
  идемпотентность: >-
    Свойство операции давать тот же результат при повторе. Второй раз ничего
    не добавляет и ничего не портит.
  проекция: >-
    Модель под чтение: данные, уложенные так, как их показывают, а не так, как
    их меняют.
  application: >-
    Уровень, который определяет, какие задачи выполняет программа. Раздаёт
    работу домену, правил предметной области не содержит и остаётся тонким.
comments:
  aggRoot: 'счёт: корень агрегата'
  aggIssue: 'создаёт счёт и считает итог из строк'
  aggIssueVer: 'создаёт счёт; версия остаётся нулевой — в базе его ещё нет'
  aggCopy: 'копия строк: снаружи их не поменять'
  aggRecord: 'записывает факт; отправлять его — не дело агрегата'
  aggEvents: 'отдаёт накопленные факты наружу'
  aggVersion: 'номер состояния, которое прочитали из базы'
  aggVersionGet: 'отдаёт версию; менять её снаружи нельзя'
  aggPaid: 'отмечает счёт оплаченным; дважды оплатить нельзя'
  aggRestore: 'собирает счёт из того, что лежало в базе'
  aggRestoreVer: 'версия приезжает вместе с данными'
  eventWhat: 'факт домена; имя уезжает на шину как тип сообщения'
  eventIssued: 'счёт выставлен'
  eventPaid: 'счёт оплачен'
  portRepo: 'порт: как счёт попадает в базу и возвращается оттуда'
  portSaveConflict: 'вернёт ErrConflict, если счёт успели изменить'
  errConflict: счёт изменили между чтением и записью
  portPublisher: 'порт: куда уезжают факты'
  portUoW: 'порт: выполнить работу целиком или не выполнять'
  moneyMinor: 'сумма в копейках'
  ucTxDeps: зависимости — только порты домена
  ucIssueRun: 'порядок операции: собрать, сохранить, рассказать'
  ucPayRun: 'достать, вызвать метод, сохранить, рассказать — одной операцией'
  ucAttempts: сколько раз пробовать при конфликте
  ucRetryRun: повторяет попытку целиком, пока счёт меняет кто-то другой
  ucRetryConflict: 'конфликт — не ошибка вызова: данные устарели'
  ucRetryOnce: 'одна попытка: достать, применить, сохранить, рассказать'
  repoWhat: реализация порта хранения на Postgres
  repoLoad: достаёт счёт и собирает агрегат
  repoLoadVer: достаёт счёт вместе с версией строки
  repoRestore: сборка агрегата — дело домена
  repoSave: пишет счёт и его строки
  repoSaveVer: 'пишет счёт, если его версия в базе всё ещё та'
  repoInsert: 'нулевая версия — счёт новый, его вставляют'
  repoUpdateVer: условие по версии стоит в самом UPDATE
  repoNoRows: ни одной строки — значит, версия в базе другая
  uowWhat: 'реализация единицы работы: транзакция Postgres'
  uowDo: вся работа внутри одной транзакции
  uowRollback: ошибка внутри — откат целиком
  uowConn: 'транзакция из ctx, если операция уже началась'
  outboxWhat: 'реализация публикации: факты строками в базе'
  outboxPublish: пишет факты в ту же транзакцию
  outboxRow: имя факта и тело сообщения
steps:
  - id: lost
    title: Два вызова, один счёт
    narration: |-
      Справа сценарий оплаты — такой, каким его оставила глава про
      инфраструктуру: [[unit of work|единица работы]], репозиторий, публикация
      фактов.

      Пока его зовут по одному, он верен. Теперь два вызова разом: один
      принимает оплату, второй начисляет пеню — планировщик просрочки проснулся
      секундой раньше.

      Оба достали счёт в одном и том же состоянии. Оба применили своё правило к
      своей копии в памяти. Оба сохранили. В базе осталось то, что записали
      вторым: пеня затёрла оплату или оплата затёрла пеню — как повезёт.

      Это [[потерянное обновление]], и транзакцией оно не
      лечится. Транзакции друг другу не помешали: каждая сделала ровно то,
      что просили, и обе закоммитились успешно. Нигде не упало, в логах чисто.
  - id: locks
    title: Почему не запереть строку
    narration: |-
      Первое, что приходит в голову: дописать `FOR UPDATE` к `SELECT` — и
      второй вызов будет ждать первого. Это [[пессимистичная блокировка]], и
      она действительно работает.

      Цена у неё такая. Строка заперта до конца транзакции: пока сценарий
      считает пеню или ходит в платёжный шлюз, все остальные по этому счёту
      стоят в очереди. Два сценария, берущие строки в разном порядке, дают
      дедлок.

      А главное — запирать удаётся только то, что происходит внутри одной
      транзакции. Человек открыл счёт, поправил срок и нажал «сохранить» через
      минуту: держать транзакцию минуту нельзя, и в момент его правки запирать
      уже нечего.

      Значит, нужно не запрещать состоянию меняться, а замечать, что оно
      изменилось.
  - id: version
    title: Версия у счёта
    narration: |-
      Версия — это номер состояния, которое прочитали. Записывая, мы называем
      базе тот номер, что видели; если там уже другой, между нашим чтением и
      записью кто-то был.

      Поле лежит в [[агрегат|агрегате]], а не в таблице самой по себе: агрегат —
      граница согласованности, значит, и счётчик изменений у него один на всё,
      что внутри. Поменяли строку счёта — версия счёта выросла.

      Правил предметной области версия не добавляет: `Issue` создаёт счёт с
      нулём, `Version()` только отдаёт, и никакого `SetVersion` нет — номер
      увеличивает тот, кто пишет. В дереве изменился и `load.go`: счёт
      возвращается из базы с тем номером, который там лежал.
  - id: conflict
    title: Конфликт — часть контракта порта
    narration: |-
      `ErrConflict` объявлен рядом с портом, и это не мелочь: сценарий обязан
      отличать «не сохранилось, потому что счёт успели изменить» от «база
      упала». Первое — обычный ход событий, второе — авария.

      Домен при этом не узнал ни про SQL, ни про `RowsAffected`. Он сказал
      ровно то, что относится к предметной области: счёт мог измениться с
      момента, когда его прочитали, и тогда запись не состоится.

      Проверять версию руками — сравнивать её в сценарии перед `Save` — смысла
      нет: между сравнением и записью пройдёт то же самое время. Проверка имеет
      силу только там, где происходит запись.
  - id: update
    title: Условие внутри записи
    narration: |-
      `UPDATE invoices SET ..., version = version + 1 WHERE number = $1 AND
      version = $2` — вот и вся [[оптимистичная блокировка]]. Условие и запись
      в одном запросе, поэтому между ними нельзя вклиниться.

      Интересна следующая строка: `RowsAffected() == 0`. Ноль обновлённых строк
      значит, что счёта с такой версией в базе нет. Для счёта, который мы
      только что прочитали, это означает одно: версия уже другая — значит,
      `ErrConflict`.

      Нулевая версия — счёт новый, его вставляют. Второе создание с тем же
      номером ловит уникальный индекс, и версия для этого не нужна.

      Чтение теперь тянет версию из той же строки, что и остальные поля: она
      часть прочитанного состояния, а не отдельный запрос.
  - id: retry
    title: Повтор вместо ошибки
    narration: |-
      Конфликт говорит: данные, на которых ты решал, устарели. Значит, правильный
      ответ — решить заново на свежих, а не отдать ошибку наружу.

      Поэтому повторяется вся попытка вместе с чтением. Повторить только запись
      нельзя: мы бы записали те же самые устаревшие данные, от которых и ушли.

      Повтор безопасен ровно потому, что попытка целиком лежит внутри одной
      единицы работы. Не сохранилось — откатилось и то, что легло в
      [[outbox]], значит, подписчики про отменённую попытку не узнали. Без этого
      каждая попытка отправляла бы факт заново, и [[идемпотентность]] стала бы
      чужой проблемой.

      Попыток три, а не бесконечность: если конфликт приходит в третий раз, это
      уже не гонка двух вызовов, а нагрузка — и отвечать на неё надо иначе.
  - id: handover
    title: Когда повторять нельзя
    narration: |-
      Повтор уместен, когда решение принимает код: `MarkPaid` заново посмотрит
      на свежий счёт и сам скажет, что оплаченный второй раз не оплачивают.

      Он неуместен, когда решение принял человек, глядя на старое состояние.
      Тогда версия уезжает наружу вместе с данными: полем в
      [[проекция|проекции]] или заголовком `ETag`, — и возвращается с запросом
      на изменение. Не сошлась — человеку отвечают «счёт изменился, посмотрите
      заново», и это правильный ответ, а не ошибка.

      Разница не в механике, а в том, кто решал. Механика одна: номер
      прочитанного состояния, проверяемый в момент записи.

      И граница: версия обнаруживает конфликт по одному агрегату. Правило,
      которое связывает два, ею не удержать — там либо один агрегат, либо
      согласованность, растянутая по времени.
  - id: files
    title: Что получилось
    narration: |-
      Щёлкни по файлу, чтобы прочитать.

      - `domains/invoice/invoice.go` — поле версии и `Version()`.
      - `domains/invoice/load.go` — версия приезжает из базы вместе с
        данными.
      - `domains/invoice/repository.go` — `ErrConflict` рядом с портом.
      - `infrastructure/postgres/invoices.go` — условие по версии и
        `RowsAffected`.
      - `applications/payment/pay.go` — повтор попытки целиком.

      Домен вырос на одно поле и одну ошибку. Всё остальное — запись, которая
      проверяет, и сценарий, который умеет начать заново.
---

Тот же сервис, что в главах про DDD: счёт, его модули, порты и адаптеры.
Домен здесь почти не меняется — у счёта появляется одно поле, и вместе с ним
ответ на вопрос, кто победит, если два вызова придут разом.
