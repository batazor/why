---
title: 'Why DDD: transport && wiring'
description: ''
problem: >-
  Сценарии написаны, порты закрыты реализациями — а позвать сервис нельзя:
  снаружи приходит JSON, а сценарий ждёт доменные сущности. И собрать это в
  работающий процесс тоже некому: `cmd/` пуст.
tags: [ddd]
order: 9
deck: why-ddd-transport
series: why-ddd
labels:
  kindEdge: граница
  request: JSON снаружи
  requestHas: строки, числа, ISO-дата
  scenario: Сценарий
  scenarioWants: ждёт доменные сущности
  noTranslator: перевести некому
  noMain: и собрать некому
terms:
  DTO: >-
    Структура из одних полей, в которой данные пересекают границу: тело
    запроса, строка таблицы, сообщение в шину. Поведения в ней нет.
  транспорт: >-
    Точка, в которой сервис вызывают: пользователь или внешняя система.
    Решений о предметной области здесь не принимают.
  application: >-
    Уровень, который определяет, какие задачи выполняет программа. Раздаёт
    работу домену, правил предметной области не содержит и остаётся тонким.
  агрегат: >-
    Кластер объектов, который меняется как одно целое, с корнем, через который
    к нему обращаются снаружи. Граница согласованности и граница транзакции.
  unit of work: >-
    Порт, который выполняет переданную работу целиком или не выполняет вовсе.
    Слова «транзакция» в домене при этом нет.
  вертикальный слайс: >-
    Модуль, нарезанный по сценарию, а не по слою: транспорт, сценарий и работа
    с агрегатом лежат вместе, потому что меняются вместе.
  wire: >-
    Генератор сборки зависимостей от Google: читает список конструкторов и
    пишет функцию, которая их связывает. В рантайме не делает ничего.
comments:
  aggRoot: 'счёт: корень агрегата'
  aggIssue: создаёт счёт и считает итог из строк
  aggCopy: 'копия строк: снаружи их не поменять'
  aggRecord: 'записывает факт; отправлять его — не дело агрегата'
  aggEvents: отдаёт накопленные факты наружу
  aggPaid: 'отмечает счёт оплаченным; дважды оплатить нельзя'
  aggRestore: собирает счёт из того, что лежало в базе
  eventWhat: 'факт домена; имя уезжает на шину как тип сообщения'
  eventIssued: счёт выставлен
  eventPaid: счёт оплачен
  portRepo: 'порт: как счёт попадает в базу и возвращается оттуда'
  portPublisher: 'порт: куда уезжают факты'
  portUoW: 'порт: выполнить работу целиком или не выполнять'
  moneyMinor: сумма в копейках
  ucTxDeps: зависимости — только порты домена
  ucIssueRun: 'порядок операции: собрать, сохранить, рассказать'
  ucPayRun: 'достать, вызвать метод, сохранить, рассказать — одной операцией'
  repoWhat: реализация порта хранения на Postgres
  repoLoad: достаёт счёт и собирает агрегат
  repoRestore: сборка агрегата — дело домена
  repoSave: пишет счёт и его строки
  uowWhat: 'реализация единицы работы: транзакция Postgres'
  uowDo: вся работа внутри одной транзакции
  uowRollback: ошибка внутри — откат целиком
  uowConn: 'транзакция из ctx, если операция уже началась'
  outboxWhat: 'реализация публикации: факты строками в базе'
  outboxPublish: пишет факты в ту же транзакцию
  outboxRow: имя факта и тело сообщения
  dtoWhat: 'тело запроса: то, в чём данные пересекают границу'
  dtoLine: строка счёта так, как её присылают
  dtoValidate: 'проверяется форма запроса, а не правила предметной области'
  dtoDomain: 'что из запроса нужно сценарию, уже в типах домена'
  dtoToDomain: переводит тело запроса в типы домена
  hWhat: 'ручка владеет сценарием и больше ничем'
  hIssue: 'POST /invoices: разобрать, перевести, позвать'
  hDecode: тело не читается — это ошибка клиента
  hToDomain: перевод в типы домена — дело пакета dto
  hCall: сценарий получает доменные сущности, а не запрос
  hFail: ошибку домена переводит в код ответа отдельная функция
  hCreated: '201 и пустое тело: номер клиент прислал сам'
  errWhat: переводит ошибку домена в код ответа
  errRule: 'правило домена нарушено — это ошибка запроса, а не сервера'
  errUnknown: 'причина остаётся в логе, наружу — код'
  routeWhat: 'адрес ручки объявляет сам модуль'
  mainByHand: сборка руками, сверху вниз
  mainAdapters: одно соединение на все адаптеры
  mainWire: сценарию передают порты, реализации выбирает сборка
  mainWired: сборка уехала в сгенерированную функцию
  wireSet: 'набор адаптеров: один на все приложения сервиса'
  wireBind: 'какая реализация стоит за портом'
  wireInject: 'что надо собрать — по типу результата'
  wireStub: 'тело-заглушка: настоящее напишет генератор'
steps:
  - id: entry
    title: Позвать некому
    narration: |-
      Справа сценарий выставления счёта — тот, каким его оставила прошлая
      глава. Он принимает номер, покупателя, строки и срок: доменные
      сущности и ничего больше.

      Снаружи приходит не это. Снаружи — JSON: номер строкой, суммы числами,
      дата в ISO. Между ними нужен перевод, и вопрос не в том, какой взять
      фреймворк, а в том, кто переводит и где этот перевод лежит.

      Второе видно в дереве: рядом появился пустой `cmd/api`. Сценарию нужны
      три реализации портов, реализациям — соединение с базой, соединению —
      строка из окружения. Сейчас этого не делает никто, и запустить сервис
      нельзя.
  - id: dto
    title: Тело запроса
    narration: |-
      [[DTO]] — структура из одних полей: то, в чём данные пересекают границу.
      Теги `json` — часть контракта ручки, и живут они здесь, а не в домене:
      домен про JSON не знает.

      Типы примитивные намеренно. `invoice.Number` снаружи не принимают:
      приехать может что угодно, и превратить строку в тип домена — работа
      [[транспорт|транспорта]].

      `Validate` проверяет форму, а не предметную область: номер не пустой,
      тело читается. «Счёт без строк» — правило домена, его проверит `Issue`;
      продублировать его здесь — значит держать два места, одно из которых
      однажды устареет.

      Лежит тело запроса в своём пакете — `transport/http/dto`. Так у ручки и у
      её контракта разные файлы и разные причины меняться: контракт правят,
      когда договорились с клиентом, хендлер — когда меняется порядок вызова. И
      имена в пакете читаются как есть: `dto.IssueRequest`.

      Там же и перевод — `ToDomain()`. Он отдаёт не агрегат, а то, из чего
      агрегат создают: номер, покупателя, строки и срок, уже в типах домена.
      Собирать счёт транспорт не вправе — это делает `Issue` в домене, и
      проверки остаются там. Зато хендлер не обрастает циклами: одна строка
      вместо шести.

      Дальше транспорта DTO не едет: внутрь уезжают аргументы сценария.
  - id: handler
    title: 'Ручка: перевести и позвать'
    narration: |-
      Одна ручка, четыре действия: разобрать тело, проверить форму, попросить
      у него доменные сущности, позвать сценарий.

      Решений о предметной области здесь нет ни одного. Итог не считается,
      статус не ставится, порядок операции не держится — всё это осталось в
      [[агрегат|агрегате]] и в [[application]]. Ручка переводит, и только.

      Посмотри, чем она владеет: `*application.UseCase` и всё. Ни базы, ни
      транзакции, ни шины. Поэтому у ручки одна причина меняться — контракт
      запроса: база, транзакция и шина меняются, не задевая её.

      Ответ — `201` и пустое тело: номер клиент прислал сам, возвращать ему
      его же данные незачем.
  - id: errors
    title: Ошибки домена в коды ответа
    narration: |-
      Самая частая ошибка транспорта — отдать пятисоткой всё, что вернул
      сценарий. Клиент тогда не может отличить свою ошибку от нашей и начинает
      повторять то, что повторять бессмысленно.

      Перевод ошибок — тоже перевод, и лежит он там же, на границе: правило
      домена нарушено — `422`, состояние конфликтует — `409`, остальное — `500`
      и запись в лог.

      Наружу не уезжает ни текст ошибки базы, ни стек: причина остаётся в
      логе, клиенту — код и короткая фраза.

      Неизвестная ошибка получает `500` намеренно. Список кодов — часть
      контракта ручки, и расширять его должен человек, а не `err.Error()`.
  - id: router
    title: Слайс знает свой маршрут
    narration: |-
      Адрес ручки объявляет модуль, а не общий роутер где-то в корне. Так
      [[вертикальный слайс]] остаётся целым: сценарий, DTO, хендлер и адрес
      лежат вместе, потому что меняются вместе.

      Мультиплексор — стандартный: с Go 1.22 он понимает и метод, и параметры
      в пути. Фреймворку здесь нечего добавить; когда понадобится — логирование,
      авторизация, лимиты, — его место в сборке, а не в модуле.

      Сборке достаётся один вызов `Register`: ни путей, ни методов она не
      знает.
  - id: wiring
    title: Собрать это некому
    narration: |-
      `cmd/api` наконец с кодом, и сборка написана руками. Читается сверху
      вниз: соединение, три адаптера, сценарий, ручка, мультиплексор, сервер.

      Хорошая новость: это и есть весь DI. Зависимости приходят аргументами
      конструкторов, реализации выбираются в одном месте, и никакого
      контейнера для этого не нужно.

      Плохая начинается со второго приложения. Рядом встанут отправщик outbox и
      консьюмер платежей: им нужна та же база и те же адаптеры, но другая
      входная точка. Эти семь строк скопируют, копии разъедутся, и через
      полгода отправщик будет работать со своим пулом соединений.

      И порядок: чтобы узнать, что `issuing.New` требует [[unit of work|единицу
      работы]], надо открыть его подпись. Забыл — компилятор скажет про
      несовпадение типов в шестой строке, а не про то, чего не хватает.
  - id: wire
    title: 'wire: та же сборка, но сгенерированная'
    narration: |-
      [[wire]] — не контейнер и не рефлексия. Это генератор: читает список
      конструкторов и пишет ровно тот код, который мы написали руками шагом
      раньше.

      `wire.NewSet` — набор адаптеров, общий для всех приложений сервиса.
      `wire.Bind` говорит, какая реализация стоит за портом: конструктор
      отдаёт `*postgres.Invoices`, а сценарию нужен `invoice.Repository`.

      `initHandler` не выполняется: файл собирается только по тегу
      `wireinject`, а его тело — заглушка. `wire` читает `wire.Build` и
      создаёт рядом `wire_gen.go` — тот коммитят, и в проде работает он.

      Что это даёт: забытую зависимость показывает генератор, с именем типа, до
      запуска; набор адаптеров один на все точки входа; сгенерированный код
      читается глазами, как свой.

      И без фанатизма. Одному приложению с пятью конструкторами `wire` не
      нужен — руками короче и понятнее. Смысл появляется, когда из одного
      набора собирают второе и третье приложение.
  - id: files
    title: Что получилось
    narration: |-
      Щёлкни по файлу, чтобы прочитать.

      - `applications/issuing/transport/http/dto/issue.go` — тело запроса и
        проверка формы.
      - `applications/issuing/transport/http/handler.go` — перевод и вызов сценария.
      - `applications/issuing/transport/http/errors.go` — ошибки домена в коды ответа.
      - `applications/issuing/transport/http/routes.go` — адрес ручки.
      - `cmd/api/main.go` — точка входа.
      - `cmd/api/wire.go` — список провайдеров для генератора.

      Цепочка замкнулась: запрос приходит на `POST /invoices`, превращается в
      доменные сущности, сценарий собирает [[агрегат]], сохраняет его и пишет
      факт — всё одной операцией.

      Домен за эту главу не изменился ни на строку. Вторая ручка добавляется
      одним файлом рядом, второе приложение — одной функцией в `wire.go`.
---

Домен описан, сценарии написаны, порты закрыты реализациями. Не хватает двух
вещей: входящего вызова и того, кто соберёт это в работающий процесс.

Одна ручка — `POST /invoices` — и сборка сервиса целиком.
