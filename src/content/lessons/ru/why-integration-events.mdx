---
title: Why integration events
description: ''
problem: >-
  Доменный факт уехал на шину как есть, со своими полями и типами. Сосед
  разобрал его и построился на нём: теперь переименование поля внутри домена
  ломает чужой сервис, и узнаём мы об этом из его алертов.
tags: [ddd, events, distributed-systems]
order: 12
deck: why-integration-events
series: why-ddd
labels:
  kindService: сервис
  ours: Billing
  oursDoes: опубликовал доменный факт
  theirs: Доставка
  theirsDoes: разобрала его как есть
  asIs: структура домена стала контрактом
  renamed: переименовал поле — сломал соседа
terms:
  доменное событие: >-
    Факт внутри контекста: его записывает агрегат, и написан он в тех же
    словах и типах, что и домен. Меняется вместе с доменом.
  интеграционное событие: >-
    Сообщение, которым сервис рассказывает о факте соседям. Отдельный тип с
    отдельной схемой: его читают чужие, поэтому меняют по правилам совместимости.
  конверт: >-
    Обёртка сообщения: идентификатор, тип, время, ключ. Данные факта лежат в
    ней отдельным телом.
  at-least-once: >-
    Доставка, которая обещает, что сообщение придёт, но не обещает, что один
    раз. Повтор — нормальный ход событий, а не сбой.
  идемпотентность: >-
    Свойство операции давать тот же результат при повторе. Второй раз ничего
    не добавляет и ничего не портит.
  outbox: >-
    Таблица, в которую факт пишется той же транзакцией, что и изменение. На
    шину его отправляет отдельный процесс, читая эту таблицу.
  unit of work: >-
    Порт, который выполняет переданную работу целиком или не выполняет вовсе.
    Слова «транзакция» в домене при этом нет.
  ограниченный контекст: >-
    Участок системы, внутри которого у слова одно значение. За его границей то
    же слово может означать другое, и перевод происходит на границе.
  DTO: >-
    Структура из одних полей, в которой данные пересекают границу: тело
    запроса, строка таблицы, сообщение в шину. Поведения в ней нет.
  application: >-
    Уровень, который определяет, какие задачи выполняет программа. Раздаёт
    работу домену, правил предметной области не содержит и остаётся тонким.
comments:
  aggRoot: 'счёт: корень агрегата'
  aggIssue: 'создаёт счёт и считает итог из строк'
  aggCopy: 'копия строк: снаружи их не поменять'
  aggRecord: 'записывает факт; отправлять его — не дело агрегата'
  aggEvents: отдаёт накопленные факты наружу
  aggPaid: 'отмечает счёт оплаченным; дважды оплатить нельзя'
  aggRestore: собирает счёт из того, что лежало в базе
  eventWhat: 'факт домена; имя у него внутреннее'
  eventIssued: счёт выставлен
  eventPaid: счёт оплачен
  portRepo: 'порт: как счёт попадает в базу и возвращается оттуда'
  portPublisher: 'порт: куда уезжают факты'
  portUoW: 'порт: выполнить работу целиком или не выполнять'
  moneyMinor: сумма в копейках
  ucTxDeps: зависимости — только порты домена
  ucIssueRun: 'порядок операции: собрать, сохранить, рассказать'
  ucPayRun: 'достать, вызвать метод, сохранить, рассказать — одной операцией'
  repoWhat: реализация порта хранения на Postgres
  repoLoad: достаёт счёт и собирает агрегат
  repoRestore: сборка агрегата — дело домена
  repoSave: пишет счёт и его строки
  uowWhat: 'реализация единицы работы: транзакция Postgres'
  uowDo: вся работа внутри одной транзакции
  uowNested: 'операция уже началась — выполняем внутри неё, без второй транзакции'
  uowRollback: ошибка внутри — откат целиком
  uowConn: 'транзакция из ctx, если операция уже началась'
  outboxWhat: 'реализация публикации: факты строками в базе'
  outboxAppend: пишет строку сообщения; про домен и контракт не знает
  pubAppender: 'порт записи: куда лечь строке сообщения'
  pubWhat: 'реализация порта публикации: переводит и отдаёт на запись'
  pubPublish: по сообщению на факт, и ни одной отправки
  pubSkip: факт без контракта наружу не уезжает
  pubAppend: строка ложится в ту же транзакцию, что и счёт
  outboxPublish: пишет факты в ту же транзакцию
  outboxAsIs: структура домена сериализуется как есть
  outboxRow: имя факта и тело сообщения
  outboxRowKey: 'тип, ключ и тело — всё, что нужно отправителю'
  intTypes: имена сообщений с версией на конце
  intIssued: 'интеграционное событие: то, что видят соседи'
  intPaid: 'полей ровно столько, сколько нужно читателю'
  envWhat: 'конверт: метаданные сообщения отдельно от данных факта'
  envKey: 'ключ порядка: сообщения одного счёта идут по порядку'
  transInternal: 'факт, у которого нет контракта наружу'
  transWhat: переводит доменный факт в сообщение
  transIssued: 'явный разбор: что уезжает, решают здесь'
  transSkip: 'новый факт наружу сам не уедет'
  inWhat: 'схема соседа, как мы её читаем'
  inInbox: 'порт: видели ли мы это сообщение раньше'
  inDeps: 'единица работы, память о сообщениях и сценарий'
  inHandle: 'разобрать, проверить повтор, позвать сценарий'
  inFirstTime: то же сообщение второй раз — ничего не делаем
  inAlreadyPaid: 'повтор не ошибка: счёт уже в нужном состоянии'
  inboxWhat: 'реализация: таблица обработанных сообщений'
  inboxRemember: вставка по уникальному id в той же транзакции
  inboxRows: вставилось — значит, сообщение новое
steps:
  - id: leak
    title: Домен уехал на шину
    narration: |-
      Справа публикация из главы про инфраструктуру. Факт уходит в
      [[outbox]] так, как он написан в домене: `json.Marshal` берёт структуру
      `Issued` целиком, а типом сообщения становится внутреннее имя
      `invoice.issued`.

      Пока это читаем только мы, всё честно. Но сообщение прочитал сосед —
      доставка подписалась и начала собирать посылку. С этой минуты внутренняя
      структура домена стала контрактом: переименовал поле `Total` — сломал
      чужой парсер; добавил поле для себя — рассказал соседям то, чего им знать
      не нужно.

      Работает это и в обратную сторону. Доставке понадобился адрес клиента,
      которого в факте нет, — и просьба добавить поле приходит в домен.

      [[доменное событие|Доменный факт]] и сообщение наружу — разные вещи, и
      меняются они с разной скоростью. Первое мы правим когда захотим, второе —
      только договорившись со всеми, кто его читает.
  - id: contract
    title: Интеграционное событие
    narration: |-
      [[интеграционное событие|Интеграционное событие]] — отдельный тип в
      отдельном пакете, и версия стоит прямо в его пути:
      `infrastructure/broker/contracts/v1`.

      Лежит он в инфраструктуре шины, а не в корне сервиса, и это не мелочь.
      Форма сообщения — деталь того транспорта, которым оно едет: у шины это
      JSON в конверте с ключом партиции, у gRPC был бы protobuf со своими
      правилами совместимости, у HTTP — тело ответа и статус. Транспортов у
      сервиса бывает несколько, и у каждого контракт свой; общий каталог
      `contracts` делает вид, что он один.

      Типы внутри примитивные: строки и `int64`. Ни `vo.Money`, ни
      `invoice.Number` за границу не уезжают — там JSON, и поля названы так, как
      их прочитает чужой сервис. Это [[DTO]] границы, и ничего больше.

      Имя сообщения — константа с версией на конце: `billing.invoice.issued.v1`.
      По нему читателю видно, откуда сообщение, про что оно и какой схемы.
      Внутреннее `FactName()` осталось своим: его мы переименуем свободно, это
      имя — нет.

      Набор полей здесь — решение, а не отражение факта. В контракт попадает то,
      что соседям действительно нужно: добавить поле потом можно, убрать —
      уже нет.
  - id: envelope
    title: Конверт
    narration: |-
      Данные факта — одно, метаданные сообщения — другое, поэтому они и лежат
      отдельно: тело в `Payload`, вокруг него идентификатор, тип, время и ключ.

      `ID` нужен получателю: доставка [[at-least-once]] обещает, что сообщение
      придёт, но не обещает, что один раз. По этому идентификатору повтор
      узнают — к этому вернёмся на входящей стороне.

      `Key` — про порядок. Брокер держит порядок внутри партиции, а не во всём
      топике; ключ, равный номеру счёта, кладёт все сообщения одного счёта в
      одну партицию: «выставлен» придёт раньше «оплачен». Между разными
      счетами порядка нет, и он не нужен.

      `Type` стоит в [[конверт|конверте]], а не только в теле: подписчик решает,
      разбирать ли тело, ещё не разобрав его.
  - id: translate
    title: Кто переводит
    narration: |-
      Перевод живёт в инфраструктуре: домен про контракт не знает и знать не
      должен. `Contract` берёт доменный факт и отдаёт готовый конверт.

      `switch` по типу факта выглядит скучно — в этом и смысл. Он явный: чтобы
      новое сообщение уехало наружу, кто-то должен добавить его здесь. Пока не
      добавил, факт остаётся внутренним, и публикация его пропускает —
      `ErrInternalFact`.

      Именно это чаще всего и забывают. Публикуешь всё подряд — и каждый новый
      доменный факт становится публичным в день, когда его написали, молча и
      без чьего-либо решения.

      Рядом — тот, кто `Contract` зовёт: `broker.Publisher`. Это он реализует
      порт `invoice.Publisher`, который домен объявил ещё во второй главе:
      перевести факт, сериализовать и отдать строку на запись. Отправлять он
      ничего не отправляет: шина в транзакцию не входит — это было в главе про
      инфраструктуру.

      Записывает строку не он, а `Appender` — узкий порт из одного метода,
      объявленный здесь же, у того, кто им пользуется. За ним стоит
      `postgres.Outbox`, и после этой главы он занимается ровно одним делом:
      кладёт строку в таблицу. Ни домена, ни контракта в его импортах больше
      нет — раньше он знал и то, и другое.

      Направление зависимостей от этого выпрямилось: знает про контракт тот,
      кто с шиной и работает, а хранилище — только про строки. Связывает их
      сборка: `wire.Bind(new(broker.Appender), new(*postgres.Outbox))`.
  - id: inbound
    title: Нам тоже присылают
    narration: |-
      Обратная сторона границы. Платёжный шлюз публикует
      `payments.payment.received.v1`, и по этому сообщению счёт становится
      оплаченным — значит, кто-то должен его принять и позвать сценарий справа.

      Сделать это плохо можно двумя способами.

      Первый: разобрать чужой JSON прямо в сценарии. Тогда [[application]]
      начинает знать формат соседа, и переименование поля в чужом сервисе
      доезжает до нашего домена.

      Второй: позвать сценарий как есть. То же сообщение приедет второй раз —
      после ретрая брокера, после перезапуска консьюмера, после переезда
      партиции. Второй раз `MarkPaid` вернёт `ErrAlreadyPaid`, консьюмер сочтёт
      это ошибкой обработки и вернёт сообщение в очередь. И так по кругу, пока
      кто-нибудь не посмотрит в логи.
  - id: consume
    title: 'Приём: перевести, вспомнить, простить'
    narration: |-
      Хендлер лежит в слайсе платежа, рядом со своим сценарием, и делает три
      вещи.

      **Переводит.** `PaymentReceived` — наша копия чужой схемы, и она стоит на
      границе. Изменится схема у соседа — правим этот файл; домен не заметит.

      **Помнит.** `Remember` отвечает «впервые» или «уже было», и вызов стоит
      внутри той же [[unit of work|единицы работы]], что и сценарий. Иначе
      между пометкой и изменением остаётся щель, ровно в которую и попадает
      повтор. Поэтому реализация `Do` получила одну строку: транзакция в `ctx`
      уже есть — значит, выполняем работу внутри неё, а не начинаем вторую.

      **Прощает.** `ErrAlreadyPaid` — не ошибка обработки: счёт уже в нужном
      состоянии, сообщение подтверждаем и идём дальше. Это и есть
      [[идемпотентность]] на входе — два одинаковых сообщения дают один
      результат.
  - id: inbox
    title: Память о сообщениях
    narration: |-
      Реализация простая: таблица с уникальным идентификатором и
      `INSERT ... ON CONFLICT DO NOTHING`. Вставилась строка — сообщение новое;
      ноль строк — это сообщение мы уже обрабатывали.

      Соединение берётся тем же `conn(ctx, db)`, что и у остальных адаптеров, —
      значит, пометка ложится в транзакцию операции. Либо счёт оплачен и
      сообщение помечено, либо ни того, ни другого.

      О чём не надо забывать: таблица растёт. Идентификаторы старше срока хранения
      сообщений в брокере не нужны — их чистят по времени, партициями, как
      обычные логи.
  - id: evolve
    title: Как менять контракт
    narration: |-
      Правило одно, и оно не про код: схему читают чужие, значит, меняем её так,
      чтобы читатель не заметил.

      - **Добавить необязательное поле** — можно. Старый читатель его
        проигнорирует.
      - **Переименовать, удалить, сменить смысл или тип** — нельзя. Это `v2`:
        новый тип сообщения, который живёт рядом с `v1`, пока последний
        читатель не переехал.

      Значит, надо знать, кто читает. Без этого `v1` не выключить никогда — и
      версии будут копиться, а не сменяться.

      И граница, за которую лучше не выходить: событие говорит «что
      случилось», а не «сделай». Как только в контракте появляется
      `ChargePenalty`, это уже не факт, а команда — и сосед начинает управлять
      нашим доменом через шину.
  - id: files
    title: Что получилось
    narration: |-
      Щёлкни по файлу, чтобы прочитать.

      - `infrastructure/broker/contracts/v1/events.go` — что видят соседи.
      - `infrastructure/broker/contracts/v1/envelope.go` — конверт: id, тип, время, ключ.
      - `infrastructure/broker/outbound.go` — перевод факта в сообщение.
      - `infrastructure/broker/publisher.go` — публикация: переводит и отдаёт
        строку на запись.
      - `infrastructure/postgres/outbox.go` — одна строка в таблицу, и
        больше ничего.
      - `applications/payment/transport/consumer/received.go` — приём чужого сообщения.
      - `infrastructure/postgres/inbox.go` — память об обработанных.

      Домен не изменился ни на строку: у него по-прежнему свои факты и своё
      имя для каждого. Всё новое легло на границу — по обе её стороны.
---

Тот же сервис, что в главах про DDD: счёт, его модули, порты и адаптеры.
Здесь появляется граница контекста — та, за которой начинаются чужие сервисы.
У факта на ней другое имя, другая форма и другие правила изменения.
