---
title: Why Lean
description: A test says "this pair is fine". Lean says it about every pair at once, or refuses to build.
problem: >-
  Two promos landed on the same item and sold it below what we paid. Each was
  approved and reviewed on its own. Nobody checked what they do together.
incidents:
  - title: 'Amazon Prime Day, July 2019: everything at $94.48'
    text: >-
      Items sold by Amazon itself, a $13,000 Canon lens among them, went out at
      $94.48. One shopper worked out that the system was meant to charge 94.48%
      of the discounted price and instead put $94.48 down as the final amount.
      A percentage confused with an absolute value — exactly what types and a
      proof catch. Amazon shipped many of those orders anyway.
    url: https://www.techspot.com/news/81028-pricing-glitch-allowed-prime-day-customers-buy-13000.html
  - title: 'Amazon UK and RepricerExpress, December 2014: everything at a penny'
    text: >-
      Automatic repricing software was supposed to keep prices just under the
      competition. Instead, between 7pm and 8pm on a Friday, a bug sent hundreds
      of items out for nothing. One seller shipped 675 items normally priced at
      £5–100 for a penny each and lost £20,000. The worse part: Amazon could not
      cancel orders already on their way — FBA warehouses shipped without ever
      looking at the price. The invariant "price never below the floor" belongs
      at the boundary of the system, not in an external repricer you trust.
    url: https://iol.co.za/technology/2014-12-18-amazon-and-the-1p-glitch/
tags: [lean, pricing, invariants]
order: 3
deck: discount-floor
comments:
  twoPrices: what we sell it for, and what we paid
  mechanics: 'two mechanics: a percentage and a flat amount'
  oneCut: 'discount in cents: −25% of 50.00 is 12.50, a flat 5.00 is always 5.00'
  oneTest: 'each of these two promos alone leaves the price above cost'
  sequential: 'one at a time: each percentage uses the already reduced price'
  clampIt: clamp the total at cost
  reportAsks: the report reads the promo engine and skips the receipt
  rulePromise: 'what the business meant all along:'
  ruleInWords: no matter how many promos, the total never drops below cost
  assumeIt: 'intro takes the premise and names it rule: "the rule holds"'
  pickThePair: 'have applies rule to these sneakers: bad is 3200 ≤ 2813'
  contradiction: 'bad is 3200 ≤ 2813, and (by decide) computes it and proves it false'
  contradictionEnd: 'absurd turns a claim and its negation into False, exact hands it to the goal'
  counterExample: '"false" — which also has to be proved'
  pool: the entire discount pool is the margin
  noOverdraw: take what you ask for, or what is left
  finalPrice: 'total: cost plus whatever is left of the pool'
  newMechanic: a new mechanic from marketing
  cashbackShare: cashback is paid back after the purchase — it takes nothing off the price
  proofUntouched: the proof is unchanged, nobody even had to open it
terms:
  SKU: >-
    Stock Keeping Unit — one sellable line in the books. The same shoe in two
    sizes is two SKUs.
  margin: >-
    The gap between the list price and what the shop paid. Everything the shop
    makes on the item.
  proof assistant: >-
    A program that checks a proof step by step and refuses to accept a wrong
    one. Besides Lean there is Coq, Isabelle, Agda.
labels:
  kindPrice: price
  kindRule: rule
  kindPromo: promo
  kindResult: result
  costFloor: cost floor
  wePaid: what we paid
  listPrice: list
  after: after
  category: category
  coupon: coupon
  budgetSpent: budget spent
  nothingLeft: nothing left to spend
  of: of
  allThereWas: all there was
  twoPromos: two promos
  eachFine: each one is fine
  paid: paid
  costWas: cost was
  stacked: stacked
  nobodyTested: nobody tested this pair
steps:
  - id: item
    title: An item has two prices
    narration: >-
      One is what we sell it for. The other is what we paid, and it lives
      elsewhere: in procurement, in the ERP, in a supplier feed. The pricing
      engine never sees it. The code stores cents: 5000 means 50.00, and 3200
      means 32.00.
    note: An item has two prices. The engine sees one.
  - id: promo
    title: Marketing sets up the mechanics
    narration: >-
      A percentage and a flat amount. Marketing configures both in an admin
      panel, bypassing the deploy and the code review: no developer ever hears
      about it.
    note: Promos ship through the admin panel, past review.
  - id: cut
    title: The size of one discount
    narration: >-
      `cut` takes a price and one promo and returns the discount amount.
      Percentages use the price passed in. Money is stored in cents, so division
      rounds the discount down to a whole cent. A flat amount does not look at the
      price at all.
    note: One promo, one discount amount.
  - id: engine
    title: How they stack
    narration: >-
      `stack` walks the list and hands `cut` the already reduced price each time.
      After the first 25% discount, 37.50 remains; the next promo uses that
      amount. Nothing here mentions cost.
    note: The engine never hears about cost.
  - id: one
    title: One promo
    narration: >-
      The category sale and the coupon each offer 25% off. On its own, each
      reduces 50.00 to 37.50, above the cost of 32.00. These are the very same two
      promos we will combine next: both individual tests pass.
    note: Each promo leaves 37.50, above cost.
  - id: stack
    title: What if two land together?
    narration: >-
      One marketer set up the category sale in May. Another set up the coupon in
      June. Each is safe on its own. And together? There are 30 promos and 435
      pairs, and nobody tested this pair. After the first promo, 37.50 remains
      against a cost of 32.00. Do you think the coupon will keep the price above
      cost?
    note: Both passed. And together?
    drag:
      chip: −25% coupon
      slot: apply the coupon
      action: Apply coupon
      applied: Coupon applied
      result: >-
        The result is 28.13, which is 3.87 below cost. The second 25% uses 37.50: the discount of 9.375 rounds down to 9.37. Combining two individually safe promos breaks the rule.
  - id: clamp
    title: Clamp it at cost
    narration: >-
      The customer now pays 32.00. But the report still calculates its discount
      through the old engine, bypassing the receipt: 3200 paid, 2187 reported as
      discounted, 1800 actually discounted. Two different calculations cause the
      mismatch, not `max` itself. Reading the discount from the final receipt
      fixes the reporting issue too.
    note: The number is fixed. The report is not.
  - id: rule
    title: State the rule
    narration: >-
      A theorem is a claim the compiler is obliged to check. First it has to be
      written down. Here is what the business wants: for any item and any set of
      promos, the total stays at or above cost. The `∀` reads "for all",
      and that is what separates this from a test, which speaks about one case.
      So far it is only a statement: neither proved nor disproved.
    note: The rule is written. Nothing checked yet.
  - id: refute
    title: Assume it holds
    narration: >-
      The pair already dropped the price to 28.13, but so far that is an
      observation: the compiler knows nothing about it and will not bring it up
      the next time someone edits the engine. To make it know, you state the
      negation of the rule as a theorem and prove it. In Lean `¬P` is shorthand
      for `P → False`, so the job is to derive a contradiction from the
      assumption. That is what `intro` does — it takes the premise and gives it a
      name. Now `rule` is the assumption "the rule holds", and what is left to
      prove is `False`.
    note: We assumed the rule holds.
  - id: instantiate
    title: Apply it to one item
    narration: >-
      `rule` speaks about every item and every set of promos at once. Apply it to
      one — these sneakers and that very pair. Out comes `bad`: 3200 ≤ 2813. The
      numbers plainly do not add up, and that is the point — `bad` rests on the
      assumption made a step earlier, and it falls together with it.
    note: Applied to a single pair.
  - id: contradict
    title: Reach a contradiction
    narration: >-
      Take the line apart. `bad` is "3200 ≤ 2813", obtained from the assumption.
      `(by decide)` proves the opposite: Lean simply evaluates the same
      inequality and gets "false". `absurd` takes a claim together with its
      negation and produces anything at all — from a contradiction everything
      follows; what is needed from it here is `False`. `exact` hands that `False`
      to the goal, and the theorem is closed. So the assumption fails: the rule
      does not hold. The counterexample stays in the file, and the compiler
      re-runs it on every build.
    note: Disproven, with the pair attached.
  - id: budget
    title: The pool the discounts are paid from
    narration: >-
      Everything the promos may spend is the margin: the gap between the list
      price and cost. On these sneakers that is 18.00. Until now promos counted
      off the price and knew nothing about cost; now they share a wallet.
    note: The discount pool is the margin, 18.00.
  - id: spend
    title: You cannot draw on an empty wallet
    narration: >-
      `spend` takes the smaller amount: what the promo requests or what remains in
      the pool. It subtracts `taken` from both the price and the pool. The next
      percentage still uses the already reduced price. The first promo takes
      12.50; the second asks for 9.37 but receives only the remaining 5.50.
    note: A promo takes its cut or the remainder, whichever is smaller.
  - id: proof
    title: A one-line proof
    narration: >-
      The total is cost plus the remaining pool. The remainder has type `Nat` and
      cannot be negative. `Nat.le_add_right` proves that a number is no greater
      than itself plus a non-negative number. One promo gives 37.50; the pair and
      three greedy promos give 32.00. The guarantee covers the receipt total, not
      every expense the shop incurs.
    note: Total is cost plus a non-negative remainder.
  - id: evolve
    title: The next mechanic
    narration: >-
      Marketing asks for cashback. The file stops compiling until someone says
      what this mechanic takes off the price. The proof needs no rewrite: it
      never looks inside a mechanic.
    note: A new mechanic must say what it takes off.
  - id: declare
    title: Declare its share
    narration: >-
      Cashback is paid after the purchase, so it leaves the receipt total
      unchanged: `cut` returns 0. The file compiles again and the proof stays the
      same. It guarantees only that the receipt total is at least cost. A later
      cashback payment can reduce net proceeds below cost; accounting for it
      requires a different model and a separate rule.
    note: Mechanic declared. The proof is unchanged.
quiz:
  - question: Why does the report disagree after clamping in this example?
    options:
      - It is too slow on large carts
      - The report uses the old engine instead of the receipt
      - Clamping only works for percentage promos
    answer: 1
    explanation: >-
      The customer pays 32.00, making the actual discount 18.00. The report
      uses the old calculation and shows 21.87. The max function does not
      prevent correct reporting: derive the discount from the receipt total.
  - question: Why can the budget version never go below cost?
    options:
      - It checks the total after applying every promo
      - The promos can only spend a pool that equals the margin
      - It rejects promos larger than 50%
    answer: 1
    explanation: >-
      The total is cost plus whatever is left of the pool. What is left is a
      natural number and never negative, so the total never drops below cost. No
      separate check is needed after that.
---
A test is a sentence about one case. <span class="claim">"This pair of promos is
fine"</span> gets checked and passes. More promos keep arriving and the pairs
between them grow quadratically. Nobody writes that many tests.

Nothing crashes. The order goes through the normal path and shows up in
monitoring as a successful sale. Somebody finds it at month end, looking at
<span data-term="margin">margin</span> per <span data-term="SKU">SKU</span>. Or
earlier, if an employee stumbles onto the combination by accident.

**Lean** lets you write a different sentence: <span class="claim">"no set of
promos takes the price below cost"</span>. That one covers every case at once,
and it has no green-tests state — it is either proved or refuted by a specific
pair.

The compiler catches one more thing. If we ever want to add a new kind of
discount — cashback, say — the file stops compiling. The function that computes
the size of a discount matches on the promo type case by case, the new case has
no branch, and Lean names the missing one out loud. Until that branch is written,
until somebody decides what cashback takes off the price, there is no build. The
rule itself is never at risk: the proof never looks inside a mechanic and
bounds the receipt total. Later cashback payments are outside that guarantee.

## Where else rules add up

Shipping rules, tariff plans, loyalty tiers, marketplace commission schedules.
Anywhere rules compose and nobody owns the sum.

## Where the line is

You can ship pricing without a <span data-term="proof assistant">proof
assistant</span>. The question is what kind of sentence you need.
<span class="claim">"This pair is fine"</span> is a test, and there will never be
enough of them. <span class="claim">"No set of promos takes the price below
cost"</span> is what the business asked for in the first place.

The proof only bounds the receipt total. Later cashback payments, shipping,
and other expenses are outside the model, so the theorem does not establish
profitability. If the list price starts below cost, this model raises the total
to cost even without promos — a chosen policy, not a property of Lean.

The `Pricing.lean` file from this write-up compiles as a whole: Lean 4.34, no
errors. The numbers in the output are what the compiler printed.
