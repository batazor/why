---
title: DDD in action
description: ''
problem: >-
  Every service is arranged its own way: its own directory layout, its own words
  for the same things, its own answer to a problem already solved twice next
  door. What you know about one service does not carry to the next — not for the
  author, not at review, not for whoever arrives a year later.
tags: [ddd]
order: 4
deck: ddd-in-action
draft: true
terms:
  C1: >-
    The system as a whole, one block, and everything that talks to it: people
    and neighbouring systems. What is inside it is not shown at this level.
  C2: >-
    Which parts the system is made of: services, databases, queues — and the
    connections between them.
  C3: >-
    What is inside one part: its components. This is where aggregates, use
    cases, ports and adapters appear.
  DDD: >-
    Domain-Driven Design. A way to describe a service in the words of its
    domain, and to agree on what each of its levels decides.
  ubiquitous language: >-
    One set of words for the whole context: a word has one meaning, and the
    code, the events, the API and the people all spell it the same way.
  bounded context: >-
    The stretch of a system inside which one word has one meaning. Outside it
    the same word may mean something else, and the translation happens at the
    border.
  DTO: >-
    A structure of plain fields in which data crosses a boundary: a request
    body, a table row, a message on the bus. It has no behaviour.
  aggregate: >-
    A cluster of objects that changes as one whole, with a root through which
    everything outside refers to it. The consistency and transaction boundary.
  invariant: >-
    A statement about state that must be true before and after every change.
    Not a check at the door but a property the aggregate keeps at all times.
  application: >-
    The level that defines the jobs the software is supposed to do. It hands
    the work to the domain, holds no domain rules and is kept thin.
  transport: >-
    The point at which the service is called: by a person or by another
    system. No decision about the domain is made here.
steps:
  - id: boundary
    title: 'C1: what a bounded context is'
    narration: |-
      The diagram on the right is [[C1]]: the system as a whole, one block.

      The three departments from the example are three contexts, and each has
      its own truth about the word "payment". Arguing which one is correct goes
      nowhere: all three are, in different places.

      A bounded context is such a place: the stretch of a system inside which a
      word has one meaning, one model, and one team answering for both.

      The language sets the boundary. Where a word starts to mean something
      else, the context has ended; the directory layout and the database schema
      have nothing to do with that boundary — they are chosen inside it.
  - id: estate
    title: What an online shop is made of
    narration: |-
      A customer walks into the shop, and inside the shop there are three areas.

      - **Catalogue** — the basket and the checkout.
      - **Billing** — what the customer owes and what has been paid.
      - **Delivery** — parcels, carriers and where the order got to.

      Three areas, three teams, three glossaries. There is one arrow so far and
      it points at the shop as a whole: who talks to whom inside comes later.
  - id: kinds
    title: Core, supporting, generic
    narration: |-
      The same three areas, now labelled by what each is to the business.

      - **Core** is what makes this shop different from the one next door: the
        catalogue and billing. Built in-house and staffed with the best people.
      - **Supporting** is needed but wins nothing: delivery.
      - **Generic** is a solved problem, the same everywhere: authentication.
        The customer logs in there first and only then walks into the shop, and
        on the diagram it stands outside the boundary: nobody builds it — it is
        bought. Writing your own login gives the shop nothing somebody else's
        would not.

      The distinction is practical: it decides where the money and the people go.
  - id: language
    title: One word, three meanings
    narration: |-
      The customer pays, and the arrow no longer points at the shop as a whole
      but at billing: billing is what owns the word "payment".

      Until the boundary was drawn, the "payment" from the example meant three
      different events, and everybody nodded in the meeting while keeping their
      own; in code that shows up like this: one service's `payment.completed` is
      about the checkmark, another's is about money in the account.

      So the first file in billing's directory is a glossary.
  - id: glossary
    title: What is inside the glossary
    narration: |-
      One line per word: the word and a single definition — the short sentence
      the team is willing to defend. Synonyms have no place here: two words with
      one meaning mean an argument that is still going.

      The table about the three departments comes first: it picks no winner, it
      says what each of the three meanings is called here.

      The glossary is a file beside the code, edited in the same commit; a wiki
      lives its own life and six months later disagrees with what the service
      does. The rule is simple: a word that is not here is not in the code
      either. Name it first, write it after.
  - id: domains
    title: 'C2: domains inside the context'
    narration: |-
      Down to [[C2]].

      A context is not one box. Inside it there are domains, each with its own
      state and its own rules. Billing has three, and all three are named with
      words from its glossary.

      - **Invoice** — what the customer owes: the lines, the total, the state.
      - **Payment** — the attempt to collect the money: authorization, capture,
        decline.
      - **Settlement** — whether the money reached our account and whether it
        matches the statement.

      Domains have kinds too: inside a core context not everything is core.
      Invoice and payment are what billing exists for; settlement is needed but
      wins nothing.

      The catalogue and delivery have gone grey: from here on the walkthrough
      is about billing, and relative to billing the neighbours are external
      services. What is inside them, and where they keep it, is none of
      billing's business.
  - id: storage
    title: A database per domain
    narration: |-
      A domain's state lives in a database of its own. Three domains, three
      databases, and no shared one.

      Which buys the thing that matters: "do not read somebody else's tables"
      stops being an agreement people forget while on call. There are no other
      tables next door — invoice physically cannot read payment's.

      The price is just as clear: two domains can no longer be changed in one
      transaction. So consistency between them is asynchronous, which is the
      next step.
  - id: bus
    title: A bus for asynchronous interaction
    narration: |-
      Billing is back as one card here, without its domains: on the bus it is
      services that talk.

      The catalogue does not call billing — it publishes the fact that an order
      was placed. Billing reads that fact and issues an invoice. A paid invoice
      it publishes as a fact in turn, and delivery reads it and starts packing.

      Who is subscribed is not the publisher's business. Which buys the thing
      that matters: a second listener appears, and billing does not change.
  - id: provider
    title: Who moves the money
    narration: |-
      Billing does not move the money. It asks the provider and waits for the
      answer — a synchronous call, and a decline is an answer too.

      The provider is outside the boundary: it is not ours, its glossary is not
      ours, and the conversation goes through a translation. Billing keeps its
      own state regardless — what was asked and what came back is written down
      here, not only at the provider.

      That finishes the context diagram: what the context owns, where its state
      is, who it calls and who it tells. Next, billing's own C2: the parts it
      is built of inside.
  - id: overview
    title: 'C2: the whole picture'
    narration: |-
      [[C2]] in full: billing with its three domains and their databases, the
      neighbours in the shop, the bus, and the external services —
      authentication and the payments provider.
  - id: service
    title: 'C2: one domain and nothing else'
    narration: |-
      From here only billing is left, and inside billing only the invoice.

      The shop with its neighbours has been shown and is no longer needed in the
      frame. Payment and settlement were there to show that a context is not one
      domain: several services live inside its boundary, each owning its own
      front of work — issue the invoice, take the money, reconcile with the
      statement. From here the story runs through the invoice, and the rest
      would only distract.

      What remains is one domain, its database, and the bus it tells that an
      invoice was paid. From this frame on, it is code.

---

import Levels from '../../../components/Levels.astro';

<Levels
  levels={[
    {
      key: 'transport',
      name: 'Transport',
      decides: 'where we are called, and which code we answer with',
      text: `The point at which the service is called: by a person or by another system. Data arrives here, and the answer to the request leaves from here.

No decision about the domain is made at this level — transport translates: protobuf, JSON or Avro into the words of the domain and back, through a [[DTO]], one per protocol.

The DTO goes no further: what reaches application is a command in the words of the domain rather than whatever the caller sent.`,
    },
    {
      key: 'application',
      name: 'Application',
      decides: 'what jobs the software does',
      text: `The level that defines the jobs the software is supposed to do. One business scenario is one use case with its own logic: take a sale, issue an invoice, refund money.

The logic here is the order of the steps: fetch the aggregate, call it, save it, announce what happened.

No domain rules live at this level and it checks none: application hands the work to the domain and holds only the progress of the operation — the transaction, the permissions, idempotency.`,
    },
    {
      key: 'aggregate',
      name: 'Aggregate',
      decides: 'which states are possible',
      text: `A cluster of objects that changes as one whole, and a root through which everything outside refers to it.

The point of that boundary is the [[invariant]]: a statement that has to hold after every change. The lines add up to the invoice total; an invoice that is already paid does not change its amount.

The aggregate checks its own invariants, inside one transaction — which is why from the outside it cannot be left in a state the domain does not allow.`,
    },
    {
      key: 'repository',
      name: 'Repository',
      decides: 'how the aggregate reaches storage and comes back',
      text: `The port is declared by the domain: take an aggregate by id, save it together with the facts it produced.

The implementation is written by infrastructure — rows, indexes, one transaction; a table row here is the same kind of [[DTO]], only for the database.

Knowledge runs from the outside in: the database knows about the aggregate, the aggregate knows nothing about the database.`,
    },
    {
      key: 'context',
      name: 'Bounded context',
      decides: 'what we own and what we call it',
      text: `The boundary inside which a word has one meaning. Everything else lives outside it and is translated at it.

This decision comes before the others: until it is said what the context owns and which words it uses for that, there is nothing to build the other levels out of.`,
    },
  ]}
>
**<span data-term="DDD">DDD</span>** answers that with a single agreement: a
service has levels, and each level decides exactly one thing. Knowledge only
points down: a level knows the one below it and nothing about the one above.
That is why the domain depends on neither the database nor the protocol it was
called over.
</Levels>

What follows is a walkthrough of one example. An online shop: the catalogue,
delivery, customers and billing.

Say "payment" inside it and every department hears its own thing.

- **Product.** The purchase went through, the customer saw the checkmark.
- **Processing.** The money is held, nothing was captured.
- **Finance.** The money will show up in a statement two days later, or will
  not show up at all.

Three different events under one word — and that is where the walkthrough
starts.
